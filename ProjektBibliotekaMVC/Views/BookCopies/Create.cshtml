@using ProjektBibliotekaMVC.Utility;
@model ProjektBibliotekaMVC.Models.BookCopy

@{
    ViewData["Title"] = "Create";
}
@Model.IdBook
@Model.Status
@Model.Book
<h1>Create</h1>

<h4>BookCopy</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @ViewBag.Title
            <input asp-for="IdBook" class="form-control"/>
            @*<div class="form-group">
                <label asp-for="IdBook" class="control-label"></label>
                <select asp-for="IdBook" class="form-control" asp-items="ViewBag.IdBook"></select>
            </div>*@
            @*<div class="form-group">
                <label asp-for="Status" class="control-label"></label>
                <input asp-for="Status" class="form-control" disabled value="@SD.BookInMagazine"/>
                <span asp-validation-for="Status" class="text-danger"></span>
            </div>*@
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
